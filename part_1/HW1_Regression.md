# 📊 Отчет по HW1: Регрессия - предсказание цен на автомобили

## 1. EDA (Исследовательский анализ данных)

### Что было сделано
```
✅ Загрузка: train(6999×13), test(1000×13)
✅ Обзор: info(), describe(), shape, dtypes, примеры данных
✅ Отчет ydata_profiling
✅ Визуализация:
  - Pairplots по всем данных
  - Барплот - цена в зависимости от типа продавца и владельца
  - Барплоты - цена по fuel/seller_type/transmission/owner
  - Корреляционная матрица числовых признаков
✅ Проверка: дубликаты (явные и неявные), пропуски
✅ Ответы на вопросы по заданиям
✅ Анализ графиков
```

### С какими результатами
- Средний год производства около 2013, разброс от 1983 до 2020 (train) и 1995 до 2020 (test).
- Средняя цена примерно 522 тысячи для train и 617 тысяч для test, с большим разбросом (максимумы достигали миллионов), что показывает разнообразие сегментов.
- Пробег (km_driven) в среднем около 74 тысячи у train и 72 тысячи у test, максимальные значения сильно отличаются (до 2.36 млн в train, 375 тыс в test), возможно выбросы или редкие случаи.
- Расход топлива (mileage) около 19.4 и 19.3 для train и test, с похожим распределением.
- Объём двигателя (engine) с разбросом от 624 до 3604 куб.см.
- Количество мест (seats) преимущественно около 5, но в train есть значения до 14, в тесте до 9.
- В категориальных признаках fuel, seller_type, transmission, owner уникальные значения совпадают в train и test по количеству, что хорошо для обработки категориальных значений.
- В train самая частая модель — Maruti Swift Dzire VDI, в тесте — Maruti Alto 800 LXI - одна марка.
- km_driven - есть выборы с большим значением и низкими ценами, в остальном - есть "граница" km_driven, после которой цены только низкие, а до нее - могут быть любые
- max_pover и engine имеют положительную корреляцию
- чем больше год (новее машина) - тем больше машин с высоким mileage, max_pover и engine и тем максимальный mileage, max_pover и engine выше от года к году.
- между mileage с max_pover и engine прослеживается отрицательная взаимосвязь
- Сильно выделяется `Машина с тест-драйва`, как я понимаю - из салона от диллеров - у них самая высокая цена. далее идет убывание цены в зависимости от количества владельцев - чем больше, тем ниже цена.
- самые дорогие машины - с дизелем
- дороже всего машины от диллеров - из салона, а на вторичном рынке - от первоначальных владельцев. Чем больше было владельцев, тем машины дешевле (чаще всего они и старше)
- машины на автоматической коробке передач дороже, чем на ручной
- train и test очень визуально похожи, все тенденции, формы распределения сохраняются. 

### Что не вышло
❌ torque возможно обработан не оптимально
❌ не было работы с выбросами
❌ уже после обучения - хорошо было бы построить график по маркам (брендам) авто

---

## 2. Обучение на вещественных признаках (6 признаков)

### Что было сделано

✅ Выбор признаков: year, km_driven, mileage, engine, max_power, seats

✅ 6 моделей:
  1. LinearRegression (без StandardScaler)
  2. LinearRegression + StandardScaler
  3. Lasso (GridSearch 18 alpha)
  4. ElasticNet (GridSearch 126 комбинаций alpha + l1)

✅ Метрики: R², MSE, бизнес метрика = доля ошибок>10%

✅ Анализ значимости признаков

### С какими результатами

| Модель | R²_test | Business_metric | MSE_test |
|--------|---------|-----------------|----------|
| LinearRegression | **0.594** | **0.227** | 233297548205 |
| +StandardScaler | 0.594 | 0.227 | 233297548205 |
| Lasso(α=25k) | 0.566 | 0.232 | 249328962818 |
| ElasticNet | 0.562 | 0.247 | 252048043341 |

ТОП коэффициенты LinearRegression:
```
max_power: +323k/bhp
year: +153k/год  
km_driven: -44k/км
engine: +61k/CC
```


### Что дало наибольший буст

**Стандартизация** (дала только стабильность коэффициентов, а не прирост метрик):
```
Без scaler: max_power доминирует (шкалы разные)
Со scaler: все признаки сбалансированы
```

**Базовая LinearRegression** лучше регуляризации:
- Lasso с очень большим α обнулил "слабые" признаки (mileage/seats) - обычно такой α не берут. меньшие коэффициенты α также не улучшили результат


### Что не вышло
❌ Подбор гиперпараметров, использование elasticNet и lasso увеличили разрыв между метриками на обучающей выборке и тестом, а также уменьшили метрику (R² -5%)


---

## 3. Обучение на всех признаках

### Что было сделано
```
✅ Feature Engineering:
  - name (модель авто) → бренд (марка авто)
  - Pipeline: OHE(fuel/seller/transmission/owner)
  - Pipeline (тест TargetEncoder): TargetEncoder(name) + OHE(fuel/seller/transmission/owner) 
✅ 4 финальные модели:
  1. Ridge + GridSearch (OHE) (18 alpha)
  2. ElasticNet + GridSearch (OHE) (126 комбинаций alpha + l1)  
  3. Ridge + TargetEncoder(name) только name
✅ Полный анализ значимости признаков
```

### С какими результатами

🏆 ЛИДЕРЫ:

| Модель | R²_train | R²_test | Business_metric | Прирост от вещественных признаков |
|--------|----------|---------|-----------------|------------------|
| ElasticNet GS | 0.768 | 0.776 | **0.309** | +30% |
| **Ridge GS** | **0.771** | **0.781** | 0.307 | **+31%** |
| Ridge TargetEnc | 0.766 | 0.779 | 0.304 | +31% |

ТОП-5 коэффициентов ElasticNet GS:
```
+2.17M: owner=Test Drive Car
+1.51M: name=Volvo
+1.09M: name=Lexus
+0.96M: name=BMW
+0.74M: name=Land
```

### Что дало наибольший буст


**Категориальные фичи (+32% R²) :**
```
Вещественные: R²=0.594 → Все фичи: R²=0.781
Business: 0.227 → 0.307 (+35%)
Все вещественные признаки после появления категориальных перестали иметь значимое влияние на признаки.
Очень большое влияние имеет марка авто - хорошо, что ее получилось вытащить.
```
**GridSearch Ridge(α=0.001)**: оптимальная регуляризация


### Что не вышло
```
❌ бизнес метрика кажется очень низкой. только 30% цен на авто спрогнозированы с отклонением не более 10%
❌ R² не удалось поднять выше 0.781
```

---

## 🎯 ИТОГОВЫЕ ВЫВОДЫ

```
Лучший результат (по бизнес метрике):
✅ R² на тесте = 0.776
✅ Business_metric=0.309 (ошибки>10% в 30% случаев)
✅ Самый значимый признак - `Test Drive Car`
✅ Top-бренды имеют значительный вклад в цену
```


## Оценка разработанного сервиса
* Сервис получился удобным в использовании для одностраничного сайта.
* Получилось визуализировать распределение таргета, матрицу корреляции, зависимость таргета от категорий и pairplotы для числовых признаков
* Ограничения связаны с данными - получать можно датасет только в том же формате, в котором был исходный. Иначе - ошибка. В остальном не заметила серьезных ограничений, сложно и интересно было выбирать и строить графики, остальное было проще. 
* Добавить анализ тестового датасета, добавить больше интерактивных графиков - например, интерактивный график распределения признаков, чтобы можно было выбирать, что визуализировать.
какие ограничения или проблемы вы заметили;
